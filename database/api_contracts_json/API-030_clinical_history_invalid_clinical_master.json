{
  "functionality_id": "API-030",
  "module": "Clinical History",
  "functionality": "Invalid Clinical Master",
  "http_method": "POST",
  "endpoint": "/api/organizations/{organizationId}/branches/{branchId}/patients/{patientId}/clinical-history",
  "description": "Documents the specific error scenario of API-026 (Create Clinical History) where clinicalId does not reference an existing clinical_master row. Same endpoint as API-026, isolated here to preserve the original docs/API_Contracts_and_Test_Results.xlsx functionality numbering.",

  "request": {
    "path_parameters": { "organizationId": "uuid, required", "branchId": "uuid, required", "patientId": "uuid, required" },
    "query_parameters": {},
    "headers": {},
    "body": { "clinicalId": "uuid - invalid/non-existent in this scenario" }
  },

  "required_fields": ["clinicalId"],

  "validation": ["clinicalId must reference an existing clinical_master row (patient_clinical_history.clinical_id NOT NULL FK)"],

  "success": { "status_code": 0, "response": {}, "status_definition": "N/A - this functionality ID documents only the failure path; see API-026 for the success contract" },

  "errors": [
    {
      "condition": "clinicalId does not reference an existing clinical_master row",
      "status_code": 404,
      "response": { "success": false, "error": "Clinical master record not found" },
      "source": "database/api_contracts_test/012_clinical_history.sql (API-T085)"
    }
  ],

  "database": {
    "tables": ["patient_clinical_history", "clinical_master"],
    "relationships": ["patient_clinical_history.clinical_id -> clinical_master.id"],
    "constraints": ["clinical_id NOT NULL"]
  },

  "scope": { "organization_required": true, "branch_required": true },

  "e2e_reference": ["database/api_contracts_test/012_clinical_history.sql (API-T085)"],

  "test_ids": ["API-T085"]
}
