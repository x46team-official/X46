{
  "functionality_id": "API-126",
  "module": "Test Package Management",
  "functionality": "Duplicate Test in Package",
  "http_method": "POST",
  "endpoint": "/api/organizations/{organizationId}/branches/{branchId}/test-packages/{packageId}/tests",
  "description": "Documents the specific error scenario of API-121 where the request fails this validation. Same endpoint as API-121, isolated here to preserve the docs/API_Contracts_and_Test_Results.xlsx functionality numbering.",
  "request": {
    "path_parameters": {},
    "query_parameters": {},
    "headers": {},
    "body": {
      "testId": "'CBC001' test's uuid - already a member of this packageId in this scenario"
    }
  },
  "required_fields": [
    "testId"
  ],
  "validation": [
    "(packageId, testId) must be unique - a test cannot be added twice to the same package"
  ],
  "success": {
    "status_code": 0,
    "response": {},
    "status_definition": "N/A - this functionality ID documents only the failure path; see the primary add-test contract for the success response"
  },
  "errors": [
    {
      "condition": "testId already added to this packageId",
      "status_code": 409,
      "response": {
        "success": false,
        "error": "Test already in package"
      },
      "source": "database/api_contracts_test/023_test_package.sql (API-T185)"
    }
  ],
  "database": {
    "tables": [
      "test_package_test_mapping"
    ],
    "relationships": [],
    "constraints": [
      "uq_test_package_test_mapping UNIQUE (organization_id, branch_id, package_id, test_id)"
    ]
  },
  "scope": {
    "organization_required": true,
    "branch_required": true
  },
  "e2e_reference": [
    "database/api_contracts_test/023_test_package.sql (API-T185)"
  ],
  "test_ids": [
    "API-T185"
  ]
}
