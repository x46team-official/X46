{
  "functionality_id": "API-061",
  "module": "Payment",
  "functionality": "Invalid Billing ID",
  "http_method": "POST",
  "endpoint": "/api/payments",
  "description": "Documents the specific error scenario of API-057 (Create Payment) where billingMasterId does not reference an existing bill. Same endpoint as API-057, isolated here to preserve the original docs/API_Contracts_and_Test_Results.xlsx functionality numbering.",

  "request": {
    "path_parameters": {},
    "query_parameters": {},
    "headers": {},
    "body": { "billingMasterId": "uuid - invalid/non-existent in this scenario" }
  },

  "required_fields": ["billingMasterId"],

  "validation": ["billingMasterId must reference an existing billing_master row (payment.billing_master_id NOT NULL FK)"],

  "success": { "status_code": 0, "response": {}, "status_definition": "N/A - this functionality ID documents only the failure path; see API-057 for the success contract" },

  "errors": [
    {
      "condition": "billingMasterId (or the accession it implies) does not reference an existing row",
      "status_code": 404,
      "response": { "success": false, "error": "Billing not found" },
      "source": "database/api_contracts_test/015_payment.sql (API-T120)"
    }
  ],

  "database": {
    "tables": ["payment", "billing_master"],
    "relationships": ["payment.billing_master_id -> billing_master.id"],
    "constraints": ["billing_master_id NOT NULL"]
  },

  "scope": { "organization_required": true, "branch_required": true },

  "e2e_reference": ["database/api_contracts_test/015_payment.sql (API-T120)"],

  "test_ids": ["API-T120"]
}
